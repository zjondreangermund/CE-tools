name: August 23 Setting-Out CAD Feedback

on:
  push:
    branches:
      - fix/sep17-c3d2023-compile
    paths:
      - '.github/workflows/aug23-setting-out-cad-feedback-regression.yml'

permissions:
  contents: write

jobs:
  repair-c3d2023-compile:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repair branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Patch compile blockers and stale repair templates
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          import sys

          changed = []

          def read_text(path: Path):
              try:
                  with path.open('r', encoding='utf-8', newline='') as fh:
                      return fh.read()
              except (UnicodeDecodeError, OSError):
                  return None

          def write_text(path: Path, text: str):
              with path.open('w', encoding='utf-8', newline='') as fh:
                  fh.write(text)
              changed.append(str(path))

          grading = Path('src/CE.Tools.Civil3D/August23PlatformDynamicGradingCommands.cs')
          road = Path('src/CE.Tools.Civil3D/RoadCorridorCompletionCommands.cs')
          sewer = Path('src/CE.Tools.Civil3D/SewerProductionCommands.cs')

          stale_color = 'short aci = source.ColorIndex;'
          fixed_color = 'short aci = (short)source.ColorIndex;'

          # CS0266: ColorIndex is Int32; the saved ACI value is Int16.
          text = read_text(grading)
          if text is None:
              raise SystemExit(f'Unable to read {grading}')
          if stale_color in text:
              if text.count(stale_color) != 1:
                  raise SystemExit(f'Expected one ColorIndex blocker in {grading}, found {text.count(stale_color)}')
              text = text.replace(stale_color, fixed_color)
              write_text(grading, text)
          elif fixed_color not in text:
              raise SystemExit(f'Expected ColorIndex assignment not found in {grading}')

          # CS1061: RecordGraphicsModified belongs to Entity, not DBObject.
          def patch_graphics(path: Path):
              text = read_text(path)
              if text is None:
                  raise SystemExit(f'Unable to read {path}')
              if 'graphicsEntity.RecordGraphicsModified(true);' in text:
                  return
              newline = '\r\n' if '\r\n' in text else '\n'
              pattern = re.compile(r'(?m)^([ \t]*)value\.RecordGraphicsModified\(true\);\r?$')
              matches = list(pattern.finditer(text))
              if len(matches) != 1:
                  raise SystemExit(f'Expected one DBObject graphics blocker in {path}, found {len(matches)}')
              def repl(match):
                  indent = match.group(1)
                  return (
                      indent + 'Entity graphicsEntity = value as Entity;' + newline
                      + indent + 'if (graphicsEntity != null)' + newline
                      + indent + '    graphicsEntity.RecordGraphicsModified(true);'
                  )
              text = pattern.sub(repl, text, count=1)
              write_text(path, text)

          patch_graphics(road)
          patch_graphics(sewer)

          # Stop late installer/finalizer scripts from restoring the exact obsolete fragments.
          scripts = Path('scripts')
          if scripts.exists():
              for path in scripts.rglob('*'):
                  if not path.is_file():
                      continue
                  text = read_text(path)
                  if text is None:
                      continue
                  new_text = text.replace(stale_color, fixed_color)
                  new_text = new_text.replace('value.RecordGraphicsModified(true);', '((Entity)value).RecordGraphicsModified(true);')
                  if new_text != text:
                      write_text(path, new_text)

          # Validate authoritative source files.
          grading_text = read_text(grading) or ''
          road_text = read_text(road) or ''
          sewer_text = read_text(sewer) or ''
          errors = []
          if stale_color in grading_text or fixed_color not in grading_text:
              errors.append(f'{grading}: ColorIndex assignment is not compile-safe')
          bare_graphics = re.compile(r'(?m)^\s*value\.RecordGraphicsModified\(true\);\s*$')
          for path, current in ((road, road_text), (sewer, sewer_text)):
              if bare_graphics.search(current):
                  errors.append(f'{path}: DBObject RecordGraphicsModified call remains')
              if 'graphicsEntity.RecordGraphicsModified(true);' not in current:
                  errors.append(f'{path}: Entity graphics refresh call is missing')

          # In scripts, only a bare generated source line is stale; the explicit Entity cast is valid.
          if scripts.exists():
              for path in scripts.rglob('*'):
                  if not path.is_file():
                      continue
                  text = read_text(path)
                  if text is None:
                      continue
                  if stale_color in text or bare_graphics.search(text):
                      errors.append(f'{path}: stale compile-blocker template remains')

          if errors:
              print('\n'.join(errors), file=sys.stderr)
              raise SystemExit(1)

          print('Civil 3D 2023 compile blockers patched/verified:')
          for item in changed:
              print('  - ' + item)
          PY

      - name: Restore workflow files and commit repair
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout origin/main -- .github/workflows/aug23-setting-out-cad-feedback-regression.yml
          git rm -f .github/workflows/sep17-c3d2023-compile-repair.yml
          git add src/CE.Tools.Civil3D/August23PlatformDynamicGradingCommands.cs \
                  src/CE.Tools.Civil3D/RoadCorridorCompletionCommands.cs \
                  src/CE.Tools.Civil3D/SewerProductionCommands.cs \
                  scripts \
                  .github/workflows/aug23-setting-out-cad-feedback-regression.yml
          git commit -m "Fix Civil 3D 2023 compile blockers"
          git push origin HEAD:fix/sep17-c3d2023-compile
