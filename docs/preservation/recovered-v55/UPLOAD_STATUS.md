# V55 recovery archive upload

The verified archive is being uploaded in deterministic base64 chunks. Restoration runs only after the `RESTORE_READY` marker is committed.

Uploaded chunks: 00-01.
Expected chunks: 00-14.
